---
title: "Mac mini 使用日誌 02：連上了，接下來是延遲"
description: "從德國遠端操作台灣的 Mac mini，遇到 200 到 300 毫秒的延遲。記錄螢幕共享複製貼上、Tailscale 傳檔，以及透過 SSH 使用遠端 Codex CLI 的過程。"
date: 2026-09-23
category: "tech"
subcategory: "engineering"
tags: ["MacMini使用日誌", "跨國AI使用", "跨國協作", "AI應用", "台灣", "德國"]
featured: false
draft: false
---

Tailscale 接通之後，我已經可以從德國，透過螢幕共享操作台灣那台 Mac mini。

看到桌面出現在眼前，確實滿開心的。機器放在台灣，人坐在德國，現在可以開始用了。

不過，真的開始操作，距離感就出來了。

我這邊測到的 ping 大概落在 200 到 300 毫秒。用螢幕共享回去操作，有時候會感覺慢一拍。可以用，但滑鼠點下去、畫面再回應，中間那一點等待，還是感覺得到。

所以連上之後，接下來做的就是把使用過程裡的小麻煩，一個一個處理掉。

第一個很實用的設定，是讓螢幕共享兩端可以複製貼上。

我在德國這台 Mac 複製一段文字，再到台灣 Mac mini 的共享畫面裡貼上。設定好之後方便很多，尤其照著 ChatGPT 的說明操作，有些指令直接複製過去，就省掉重新打一遍的時間。

另外還有一個我很喜歡的功能：透過 Tailscale 傳檔案。

從德國這台傳到台灣那台，在接收端的下載資料夾就能拿到。這個對我來說很實用。兩台電腦放得這麼遠，有一條方便的傳檔路徑，操作起來會輕鬆不少。

接著就開始弄 Codex。

我想要的使用方式是：人在德國這邊操作，工作放在台灣 Mac mini 的本地資料夾裡進行。最好不用一直盯著遠端桌面，等它把每一次操作的畫面傳回來。

前面設定好的 SSH，這時候就派上用場了。

在德國這台 Mac 打開 Terminal，透過 SSH 連進台灣的 Mac mini。連上之後，視窗雖然開在我面前，但裡面執行的指令，已經是在台灣那台機器上跑。

這個感覺滿有意思的。手邊這台電腦，就這樣成了操作另一台機器的入口。

這一段設定稍微麻煩一點，我還是搭配 ChatGPT，一步一步處理 SSH 的 config 和連線方式。接著，透過這個 SSH 連線，在台灣的 Mac mini 安裝 Codex CLI。

這裡要分清楚：這次實際走通的方式，是從德國的 Terminal 連過去，執行裝在台灣 Mac mini 上的 Codex CLI。它就在那邊的資料夾裡工作。

換成這種操作方式之後，我感覺延遲少了很多。

德國到台灣的網路距離還是在，只是用終端機輸入指令、看文字回應，比透過螢幕共享操作整個桌面，更適合我現在要做的事情。

到這裡，這台 Mac mini 開始有了幾種不同的使用方式：

要看畫面，就開螢幕共享。

要送檔案，就透過 Tailscale。

要進資料夾做事、執行 Codex，就用 SSH 連進去。

第一階段是把機器連上。這一次，開始把它調整成我在德國也用得順手的樣子。

接下來，就可以慢慢把想做的東西放進去了。
