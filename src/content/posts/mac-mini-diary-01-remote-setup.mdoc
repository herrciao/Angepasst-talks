---
title: "Mac mini 使用日誌 01：我在德國，先讓台灣那台連上線"
description: "人在德國，買了一台放在台灣的 64GB Mac mini。從同事協助開機、安裝 Tailscale，到設定 SSH 與螢幕共享，記錄第一次跨國遠端連線的過程。"
date: 2026-09-22
category: "tech"
subcategory: "engineering"
tags: ["MacMini使用日誌", "跨國AI使用", "跨國協作", "AI應用", "台灣", "德國"]
featured: false
draft: false
---

我買了一台 64GB 記憶體的 Mac mini。

比較特別的是，我人在德國，機器買在台灣。原因很直接：比較便宜。

於是請台灣的同事幫忙收貨，再接上螢幕、鍵盤和滑鼠，協助完成最初的設定。我在德國這邊配合，兩邊一起把這台機器弄起來。

在這之前，我其實已經做了不少功課。買來要做什麼、怎麼從德國操作、有哪些東西要先裝，大概都有想過。

真正開始弄，還是遇到一些小麻煩。

最初的安排，是先建立 Mac 的本機主帳號，暫時不登入 Apple ID，再把 Tailscale 裝上去。

結果第一個卡住的地方，就是下載和登入。

當時在找 Tailscale 的下載方式，碰到了帳號登入的流程；走 Mac 的 App Store，又需要 Apple ID。原本想先不登入 Apple ID，實際操作時，還是得處理這件事。

最後請同事先用他的 Apple ID，幫忙下載 Tailscale。等到 Tailscale 登入畫面出現 QR Code，再由我這邊掃碼完成登入。

這才過了第一關。

接下來，德國這台 MacBook Air 也要裝好 Tailscale，讓兩台機器都上線。

兩邊連上之後，就能看到各自的 Tailscale IP。對我來說，這時候才開始有一點實感：台灣那台剛買來的機器，已經出現在我可以連線的範圍裡。

但後面還有事情要做。

我照著 ChatGPT 協助整理的流程，在德國這台 Mac 打開 Terminal，先用 `tailscale status` 看裝置狀態，再對台灣那台機器的 IP 做 `ping` 測試，接著嘗試 SSH 登入。

這裡有兩種操作方式要分清楚。

SSH 連進去之後，可以透過終端機執行指令。想看到 Mac 的桌面、用滑鼠操作，就要另外使用螢幕共享。

所以台灣那台 Mac mini，也得先把相關設定準備好。

我這次處理的項目包括：

- 防止顯示器關閉後，電腦自動進入睡眠。
- 開啟「喚醒以供網路存取」。
- 開啟停電後自動啟動。
- 在系統設定的「共享」裡，開啟遠端登入與螢幕共享。
- 關閉自動更新。
- 設定帳號自動登入。

這份是我這次採用的設定紀錄。尤其自動更新和重新開機之後的登入狀態，是我當時特別留意的地方。

畢竟機器放在台灣，不能每次連不上，就自己走過去看一下。

SSH 接通之後，後面要裝 Git、Python，或是其他工具，就可以從德國這邊繼續操作，不用每一步都請同事坐在螢幕前幫忙。

不過，當下連得進去，還只是第一階段。

最後還要驗收一件事：Mac mini 重新開機之後，Tailscale 能不能恢復連線，讓我從德國再次登入。

我當時也把自動登入納入設定，希望機器重開之後，能回到遠端可操作的狀態。這一段的實際結果，還要另外確認。

這次先做到這裡：同事在台灣接螢幕、協助設定，我在德國掃碼、測試連線，再慢慢接手後面的操作。

一台買在台灣的 Mac mini，就這樣開始讓我從德國使用了。
