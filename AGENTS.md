# Codex 專案操作聲明

本專案由 Codex 協助維護，但專案擁有者保留所有變更的最終決定權。

## 強制確認原則

任何人或 AI agent 在修改本專案之前，都必須先向專案擁有者說明預計修改的內容、涉及的檔案及可能影響，並取得專案擁有者在當次對話中的明確確認。

沒有明確確認時，只能進行唯讀檢查、分析與回報，不得推定沉默、過去的授權或概括性要求等同於本次修改許可。

## 必須先確認的操作

以下操作一律需要專案擁有者事前明確確認：

- 新增、修改、移動、重新命名或刪除任何檔案。
- 執行會自動改寫檔案的格式化、生成、遷移或修復工具。
- 安裝、移除或升級套件與依賴。
- 修改設定、內容、文章、網站介面、資料或自動化流程。
- 執行 `git add`、建立 commit、切換或建立分支、合併、rebase、reset、push 或建立 pull request。
- 發布、部署、重新部署或觸發任何外部服務及內容發布流程。
- 操作帳號、憑證、密鑰、環境變數或第三方整合。

## 允許的唯讀工作

未取得修改許可前，可以進行不改變任何狀態的檔案閱讀、搜尋、Git 狀態檢查、差異分析及進度回報。

如果無法確定某項操作是否會改變狀態，必須視為會修改，先停止並詢問專案擁有者。

## 授權範圍

專案擁有者的確認只適用於當次明確說明的工作範圍，不自動延伸至其他檔案、後續任務、提交、推送或部署。完成已確認的修改後，若要進入下一個會改變狀態的階段，必須再次取得確認。

## 既有專案規則

進行寫作或內容工作時，應參考 `CLAUDE.md`、`.claude/commands/`、`.cursor/rules/` 與 `.cursor/skills/` 中既有的內容與安全規則。這些既有規則不構成修改、提交、推送或部署的授權；如有衝突，以本聲明的事前確認要求為優先。

## SEO 與公開網站保護

- 不得任意更改、搬移、合併或刪除既有公開 URL。
- URL 變更必須先取得批准，並提交 redirect、canonical、sitemap 與內部連結調整計畫。
- 不得破壞 metadata、canonical、crawlability、robots、sitemap、schema、breadcrumbs、internal links、表單、Analytics、轉換追蹤或部署設定。
- 不得以重構為理由改變既有頁面的搜尋意圖或刪除 SEO 資產。
- 同一搜尋意圖已有文章時，優先評估更新既有頁面，並記錄 cannibalization 判斷。

## 內容與事實安全

- 不得虛構案例、親身經驗、客戶、結果、數據、引用、測試或時間。
- AI 推論必須標示為推論，不得寫成已確認事實。
- 不得大量產生薄弱、重複或堆疊關鍵字的內容。
- 高風險事實、圖片、案例與資料必須經專案擁有者確認隱私、授權與可公開範圍。
- 缺少 Search Console、Analytics 或其他實際數據時，標記為「尚未提供」，不得虛構數值。

## SEO Runtime 發布關卡

內容狀態固定依序為：

`waiting-technical-review → approved → built → validated → committed → published`

AI 不得自行將 `waiting-technical-review` 改成 `approved`。每一個會改變狀態的階段，都必須遵守本文件的事前確認原則；尤其不得自行 commit、push、deploy、publish 或提交 Search Console indexing。
